pipeline {

  
  agent {
    kubernetes {
      yaml """
apiVersion: v1
kind: Pod
metadata:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::300374333822:role/jenkins-irsa-role
spec:
  serviceAccountName: jenkins-agent
  containers:
    - name: runner
      image: 300374333822.dkr.ecr.us-east-2.amazonaws.com/my-custom-kubectl:1.3
      command: ["cat"]
      tty: true
"""
    }
  }


  options {
    skipDefaultCheckout()
  }


  stages {
    stage('Prepare') {
      steps {
        echo '🚀 Starting downstream-build-3'
      }
    }

    stage('Build') {
      steps {
        echo '🔨 Simulating build in downstream-build-3...'
        sh 'sleep 5'
      }
    }

    stage('Complete') {
      steps {
        echo '✅ downstream-build-3 completed successfully!'
      }
    }
  }
  
}