pipeline {
  agent any
  stages {
    stage('Init') {
      steps {
        script {
          // Load the helper from sibling folder
          def envHelper = load '../jenkins-lib/prepareEnv.groovy'
          envHelper.run('qa')
        }
      }
    }
  }
}

