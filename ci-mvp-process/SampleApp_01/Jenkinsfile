pipeline {
  agent {
    kubernetes {
      yaml """
      apiVersion: v1
      kind: Pod
      spec:
        containers:
        - name: busybox
          image: busybox
          command:
          - sleep
          args:
          - "900"
      """
    }
  }
  stages {
    stage('Test') {
      steps {
        container('busybox') {
          sh 'echo Hello from agent && sleep 900'
        }
      }
    }
  }
}

