<?xml version="1.0"?>
<!DOCTYPE project>

<project name="build-common-ivy" xmlns:antelope="antlib:ise.antelope.tasks">
	<macrodef name="mirrors-get">
		<attribute name="dest" />
		<attribute default="false" name="ignoreerrors" />
		<attribute name="src" />
		<attribute default="true" name="trylocalnetwork" />
		<attribute default="false" name="verbose" />

		<sequential>
			<local name="src" />

			<property name="src" value="@{src}" />

			<propertyregex
				input="${src}"
				override="true"
				property="src"
				regexp="http://mirrors/"
				replace="http://mirrors.lax.liferay.com/"
			/>

			<propertyregex
				input="${src}"
				override="true"
				property="mirrors.cache.artifact.subdir"
				regexp="https?://(.+/).+"
				replace="\1"
			/>

			<property name="mirrors.cache.artifact.dir" value="${mirrors.cache.dir}/${mirrors.cache.artifact.subdir}" />

			<propertyregex
				input="${src}"
				override="true"
				property="mirrors.cache.artifact.file.name"
				regexp=".+/(.+)"
				replace="\1"
			/>

			<if>
				<not>
					<available file="${mirrors.cache.artifact.dir}/${mirrors.cache.artifact.file.name}" />
				</not>
				<then>
					<mkdir dir="${mirrors.cache.artifact.dir}" />

					<get
						dest="${mirrors.cache.artifact.dir}/${mirrors.cache.artifact.file.name}.md5"
						ignoreerrors="true"
						retries="1"
						src="${src}.md5"
					/>

					<retry retrycount="3" retrydelay="5000">
						<sequential>
							<propertyregex
								input="${src}"
								override="true"
								property="mirrors.src"
								regexp="http://"
								replace="http://mirrors.lax.liferay.com/"
							/>

							<if>
								<equals arg1="@{trylocalnetwork}" arg2="true" />
								<then>
									<trycatch>
										<try>
											<get
												dest="${mirrors.cache.artifact.dir}"
												ignoreerrors="@{ignoreerrors}"
												src="${mirrors.src}"
												verbose="@{verbose}"
											/>
										</try>
										<catch>
											<get
												dest="${mirrors.cache.artifact.dir}"
												ignoreerrors="@{ignoreerrors}"
												src="${src}"
												verbose="@{verbose}"
											/>
										</catch>
									</trycatch>
								</then>
								<else>
									<get
										dest="${mirrors.cache.artifact.dir}"
										ignoreerrors="@{ignoreerrors}"
										src="${src}"
										verbose="@{verbose}"
									/>
								</else>
							</if>


							<if>
								<available file="${mirrors.cache.artifact.dir}/${mirrors.cache.artifact.file.name}.md5" />
								<then>
									<if>
										<checksum file="${mirrors.cache.artifact.dir}/${mirrors.cache.artifact.file.name}" fileext=".md5" verifyproperty="valid.checksum" />
										<then>
											<delete file="${mirrors.cache.artifact.dir}/${mirrors.cache.artifact.file.name}.md5" />
										</then>
										<else>
											<echo>MD5 checksum failure for ${mirrors.cache.artifact.dir}/${mirrors.cache.artifact.file.name}</echo>
											
											<fail message="MD5 checksum failure for ${mirrors.cache.artifact.dir}/${mirrors.cache.artifact.file.name}" />
										</else>
									</if>

								</then>
							</if>
						</sequential>
					</retry>
				</then>
			</if>

			<if>
				<available file="@{dest}" type="dir" />
				<then>
					<copy
						file="${mirrors.cache.artifact.dir}/${mirrors.cache.artifact.file.name}"
						tofile="@{dest}/${mirrors.cache.artifact.file.name}"
					/>
				</then>
				<else>
					<copy
						file="${mirrors.cache.artifact.dir}/${mirrors.cache.artifact.file.name}"
						tofile="@{dest}"
					/>
				</else>
			</if>

			<var name="mirrors.cache.artifact.dir" unset="true" />
			<var name="mirrors.cache.artifact.file.name" unset="true" />
			<var name="mirrors.cache.artifact.subdir" unset="true" />
			<var name="mirrors.src" unset="true" />
		</sequential>
	</macrodef>
</project>